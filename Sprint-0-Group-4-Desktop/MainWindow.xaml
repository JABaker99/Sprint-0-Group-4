<Window x:Class="Sprint_0_Group_4_Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sprint_0_Group_4_Desktop"
        mc:Ignorable="d"
        Title="Users" Height="450" Width="850"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Users from Database"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"/>

        <Button Grid.Row="1"
                Content="Refresh"
                Width="110"
                HorizontalAlignment="Left"
                Margin="0,0,0,12"
                Click="Refresh_Click"/>

        <DataGrid Grid.Row="2"
                  x:Name="UsersGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  Visibility="{Binding UsersTableVisibility}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding Firstname}" Width="*"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding Lastname}" Width="*"/>

                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2"
                   Text="No users found"
                   FontSize="16"
                   Foreground="Gray"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding NoUsersTextVisibility}"/>
    </Grid>
</Window>